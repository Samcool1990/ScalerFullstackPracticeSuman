function main(book) {
    // Write your code here
    // Use console.log to output the result  
    var format_string = `${book.title} by ${book.author} was published in ${book.publishedYear}.`;
    console.log(format_string)
}